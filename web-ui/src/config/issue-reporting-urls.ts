/**
 * Where bug reports / issues are filed for this fork.
 *
 * This is a fork of upstream cline/kanban maintained at RealmX1/cline-kanban, so issues
 * must go to the fork's tracker — not upstream. Both the sidebar "Report issue" footer and
 * the bug-report FAB's GitHub-issue fallback link here.
 */
export const KANBAN_FORK_GITHUB_REPO_URL = "https://github.com/RealmX1/cline-kanban";
export const KANBAN_FORK_GITHUB_ISSUES_URL = `${KANBAN_FORK_GITHUB_REPO_URL}/issues`;

/**
 * The GitHub `owner/repo` slug (lowercase) that identifies the cline-kanban developer
 * project. A Kanban project whose git origin normalizes to this slug is treated as the
 * "developing Kanban with Kanban" workspace, enabling the bug-report FAB's auto-task option.
 */
export const KANBAN_DEV_PROJECT_GITHUB_SLUG = "realmx1/cline-kanban";
